import { SortListPipePipe } from './sort-list-pipe.pipe';

describe('SortListPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SortListPipePipe();
    expect(pipe).toBeTruthy();
  });
});
